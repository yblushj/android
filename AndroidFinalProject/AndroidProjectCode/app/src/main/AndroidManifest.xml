<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.xiaocaiyidie">

    <application
        android:allowBackup="true"
        android:icon="@drawable/theme_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.XiaoCaiYiDie">
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <activity
            android:label="搜索"
            android:name=".SearchActivity" />

        <activity
            android:label="设置"
            android:name=".SettingActivity" />

        <activity
            android:label="关注"
            android:name=".AttentionActivity"/>

        <activity
            android:label="粉丝"
            android:name=".FansActivity"/>

        <activity
            android:label="赞和收藏"
            android:name=".StarActivity"/>

        <activity
            android:label="动态"
            android:name=".TrendsActivity"/>

        <activity
            android:label="新建"
            android:name=".RecipeAddActivity" />
        
        <activity
            android:label="新建菜谱"
            android:name=".AddRecipeActiviey"/>

        <activity
            android:label="新建动态"
            android:name=".AddTrendsActivity"/>

    </application>

</manifest>